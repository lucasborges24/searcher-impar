import { createTheme } from '@mui/material';

export const buttonTheme = createTheme({
  palette: {
    newCard: {
      main: '#E76316',
      contrastText: '#FFFFFF',
      opacity: 1,
    },

    editCard: {
      main: '#263238',
      contrastText: '#fff',
    },
  },
});
